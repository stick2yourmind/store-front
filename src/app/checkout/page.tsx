import CheckoutClient from '@/app/checkout/page.client';

function CheckoutPage() {
  return (
    <main className="flex min-w-full max-w-7xl grow flex-col">
      <CheckoutClient />
    </main>
  );
}
export default CheckoutPage;
